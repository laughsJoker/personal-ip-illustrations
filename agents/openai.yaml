interface:
  display_name: "个人 IP 视觉分身"
  short_description: "创建稳定一致的个人IP视觉分身，并生成配图、封面和知识卡片"
  default_prompt: "Use $personal-ip-illustrations 为我创建个人 IP 视觉分身，并生成4张标准参考图。"
policy:
  allow_implicit_invocation: true
